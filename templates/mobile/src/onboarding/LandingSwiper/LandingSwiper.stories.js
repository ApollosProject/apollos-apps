import React from 'react';
import { storiesOf } from '@apollosproject/ui-storybook';
import LandingSwiper from './index';

storiesOf('Onboarding/LandingSwiper', module).add('full', () => (
  <LandingSwiper />
));
