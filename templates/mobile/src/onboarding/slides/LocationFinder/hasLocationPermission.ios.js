// TODO this would be better than getting the position to check
// if we could figure out a way to make it happen
// react-native-permissions can do it, but that requires allowing ALL
// permissions in the app
export default null;
