coverage:
  status:
    project:
      api:
        paths:
          - packages/apollos-church-api/*
          - packages/apollos-rock-apollo-data-source/*
          - packages/apollos-config/*
          - packages/apollos-core-api/*
      mobile:
        paths:
          - packages/apolloschurchapp/*
comment:
  layout: "reach, diff, files"
  behavior: once
