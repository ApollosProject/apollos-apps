// placeholder to avoid rewriting all icons
// had to ditch the dependency on the theme folder because the theme folder depends on the icons
const makeIcon = (icon) => icon;

export default makeIcon;
