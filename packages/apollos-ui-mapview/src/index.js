import MapView from './MapView';

export MapViewConnected, {
  GET_CAMPUS_LOCATIONS,
  CAMPUS_CHANGE,
} from './MapViewConnected';

export getUserLocation, {
  hasLocationPermission,
} from './utils/getUserLocation';

export default MapView;
