coverage:
  status:
    project:
      api:
        paths:
          - packages/apollos-church-api/*
          - packages/apollos-data-prayer/*
      mobile:
        paths:
          - packages/apolloschurchapp/*
comment:
  layout: "reach, diff, files"
  behavior: once
