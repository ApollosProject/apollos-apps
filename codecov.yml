coverage:
  status:
    project:
      api:
        paths:
          - packages/apollos-data-*/*
          - packages/apollos-config/*
          - packages/apollos-server-core/*
      mobile:
        paths:
          - packages/apollos-ui-*/*
comment:
  layout: "reach, diff, files"
  behavior: once
